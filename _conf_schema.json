{
    "at_view_pig": {
        "description": "开启艾特他人查看今日小猪功能",
        "hint": "是否允许通过艾特他人来查看对方的今日小猪，默认为关闭",
        "type": "bool",
        "default": false
    },
    "enable_new_pig_pity": {
        "description": "开启连续重复保底",
        "hint": "连续抽到已解锁小猪后，逐步提高下一次抽到新小猪的机会",
        "type": "bool",
        "default": true
    },
    "pity_step_percent": {
        "description": "每层保底增加概率（百分比）",
        "hint": "每连续重复一次增加的重抽新猪概率，范围 0-50，默认 15",
        "type": "int",
        "default": 15
    },
    "enable_roast": {
        "description": "开启今日烤猪",
        "hint": "允许把自己当天的小猪做成趣味料理卡；人类、熟食形态和已吃掉的小猪会被拦截",
        "type": "bool",
        "default": true
    },
    "enable_group_roast": {
        "description": "开启烤群友玩法",
        "hint": "群聊中可使用 /烤群友 @某人、/随机烤群友 和后门口令；关闭后仅保留今日烤猪",
        "type": "bool",
        "default": true
    },
    "enable_group_eat": {
        "description": "开启吃群友玩法",
        "hint": "允许 /吃群友 @某人 与 /随机吃群友；成功者或失败者会变为当天的「吃掉了」状态",
        "type": "bool",
        "default": true
    },
    "eat_success_percent": {
        "description": "吃群友成功率（百分比）",
        "hint": "范围 1-80，默认 15；失败时发起者会变为「吃掉了」",
        "type": "int",
        "default": 15
    },
    "eaten_next_day_failure_percent": {
        "description": "被吃后次日抽猪失败率（百分比）",
        "hint": "范围 1-80，默认 20；次日首次抽猪失败后会锁定到当天结束，成功则正常抽取",
        "type": "int",
        "default": 20
    },
    "group_roast_cooldown_hours": {
        "description": "烤群友冷却时间（小时）",
        "hint": "普通烤群友按发起者与群组分别冷却，范围 1-72，默认 8；后门口令会绕过此限制",
        "type": "float",
        "default": 8
    },
    "enable_roast_protection": {
        "description": "开启被烤保护机制",
        "hint": "群友昨天在同一群被成功烤到次数达到阈值后，今天普通烤群友会被拦截；后门强制模式仍可突破保护",
        "type": "bool",
        "default": true
    },
    "roast_protection_threshold": {
        "description": "触发次日保护的被烤次数",
        "hint": "按同一群聊统计当天实际被烤次数，范围 1-20，默认 3；逃脱不计数，反噬则计入被反噬者",
        "type": "int",
        "default": 3
    },
    "enable_ai_roast_copy": {
        "description": "启用 AI 烤猪文案",
        "hint": "每只猪每天最多调用一次当前会话模型；生成后保存 7 天，后续烤同一只猪会随机复用近 7 天文案。模型不可用、超时或报错时自动回退本地模板",
        "type": "bool",
        "default": false
    },
    "image_theme": {
        "description": "图片卡片日夜主题",
        "hint": "auto 会在 19:00-06:59 使用深色夜间主题；也可填 light 或 dark 固定主题",
        "type": "string",
        "default": "auto"
    },
    "resource_sync_enabled": {
        "description": "开启云端公共小猪资源同步",
        "hint": "每次更新只替换云端基础层，本地新增、编辑与删除屏蔽不会被覆盖",
        "type": "bool",
        "default": true
    },
    "resource_manifest_url": {
        "description": "云端资源 manifest 地址",
        "hint": "必须是 HTTPS；默认使用 rollpig-plus 公共资源包",
        "type": "string",
        "default": "https://pig.felislab.cc/resources/rollpig/manifest.json"
    },
    "resource_sync_interval_hours": {
        "description": "云资源自动同步间隔（小时）",
        "hint": "范围 1-168，默认每 24 小时检查一次",
        "type": "float",
        "default": 24
    },
    "resource_sync_timeout": {
        "description": "资源网络请求超时（秒）",
        "hint": "连接超时范围 2-120，默认 30 秒；图片读取至少允许 45 秒并自动重试",
        "type": "float",
        "default": 30
    },
    "resource_use_system_proxy": {
        "description": "云资源使用系统代理",
        "hint": "默认关闭并直连，避免失效代理导致 TLS 连接卡住；仅在服务器必须通过代理访问外网时开启",
        "type": "bool",
        "default": false
    },
    "resource_max_file_size_mb": {
        "description": "资源单文件大小上限（MiB）",
        "hint": "范围 1-50，默认 10 MiB；同时用于 PigHub 图片导入",
        "type": "int",
        "default": 10
    },
    "timezone": {
        "description": "每日边界时区",
        "hint": "填写 IANA 时区（例如 Asia/Shanghai、America/Los_Angeles）；local 使用服务器系统时区",
        "type": "string",
        "default": "local"
    },
    "ai_generation_timeout_seconds": {
        "description": "AI 文案调用超时（秒）",
        "hint": "范围 5-120，默认 45；超时自动回退本地文案",
        "type": "float",
        "default": 45
    },
    "panel_update_enabled": {
        "description": "开启管理面板安全更新",
        "hint": "仅检查并安装 casama233/astrbot_plugin_rollpig 的最新稳定 GitHub Release；不接受任意 URL、分支或预发布版本",
        "type": "bool",
        "default": true
    },
    "panel_update_timeout": {
        "description": "管理面板更新网络超时（秒）",
        "hint": "范围 5-120，默认 30；下载包另有 64 MiB、文件数量与解压体积限制",
        "type": "float",
        "default": 30
    }
}
